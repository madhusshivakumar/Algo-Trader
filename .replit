language = "python3"
run = "python -m scripts.startup"
entrypoint = "core/engine.py"

[env]
TRADING_MODE = "paper"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && python -m core.engine"]
